apiVersion: v1
kind: Service
metadata:
  name: {{ include "nafets227-backup.fullname" . }}
  labels:
    {{- include "nafets227-backup.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "nafets227-backup.selectorLabels" . | nindent 4 }}

apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "nafets227-backup.fullname" . }}-test-connection"
  labels:
    {{- include "nafets227-backup.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "nafets227-backup.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
